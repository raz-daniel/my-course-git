const firstNum = prompt(`Please Enter Number`);
const secondNum = prompt(`Please Enter Another Number`);
if (firstNum > secondNum) {
    document.write(`the Bigger Num is: ${firstNum}`);
} else {
    document.write(`the Bigger Num is: ${secondNum}`);
}
